spring:
  application:
    name: usuarios-service

  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: root
      profile: dev

appDescription: "Pragma Powerup"
appVersion: "1.0.0"