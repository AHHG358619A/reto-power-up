server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AHHG358619A/reto-power-up.git
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
          default-label: main
          search-paths: config-data

  security:
    user:
      name: root
      password: root
